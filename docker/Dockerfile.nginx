FROM nginx:alpine

COPY ../nginx/nginx.conf /etc/nginx/nginx.conf

# ✅ 只 COPY 前端静态文件（没有 PHP 文件）
COPY ../client/ /usr/share/nginx/html/

# ❌ 不 COPY server 目录（PHP 文件通过 volumes 挂载）

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
